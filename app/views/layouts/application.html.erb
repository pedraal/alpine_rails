<!DOCTYPE html>
<html>
  <head>
    <title>AlpineRails</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= render 'shared/vite_headers' %>
  </head>
  <body class="dark:bg-slate-900 bg-slate-100 dark:text-white p-4 min-h-screen <%= 'debug-screens' if Rails.env.development?%>">
    <nav class="flex justify-between">
      <div class="flex gap-2">
        <%= link_to 'Home', root_url, class: '' %>
        <%= link_to 'Lookbook', lookbook_url, class: '' %>
      </div>
    </nav>
    <%= yield %>
  </body>
</html>
